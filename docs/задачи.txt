

Чтение из файла:
1) запрос длины файла
2) запрос у ОС памяти по полученному размеру файла или N=2^32 для исходных данных
3) запрос у ОС памяти для результатов





Подсчёт количества отсчётов между значениями амплитуды:
    формат слово амплитуды, два байта значений куда движемся
    если два слова (сначала значение амплитуды, потом значение куда движемся вместе два байта) равны нулям, то выходим
    нужно просуммировать количество отсчётов и положить в массив